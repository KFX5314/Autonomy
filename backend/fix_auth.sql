-- Historical helper kept for old documentation links.
-- The app database user is now created by backend/init_db.sql.

SELECT 'No action needed: run backend/init_db.sql for the canonical schema and grants.' AS result;
